# 2 — Path Parameters

Reading dynamic values straight from the URL path.

| Sub-topic | What it covers |
|---|---|
| [2/a — Basic path parameter](a) | `item_id: int` — type conversion and validation for free. |
| [2/b — Fixed path before dynamic path](b) | Why `/users/me` must be declared before `/users/{user_id}`. |

Each folder above has its own **README.md** (how to run it, routes,
`curl`/Postman) and **STEPS.md** (line-by-line code walkthrough).
