# 3/c — POST, add two numbers, step by step

1. **Model two numbers**: `a: float`, `b: float`.
2. **Return their sum**: `{"result": request.a + request.b}`. Nothing
   new here beyond reading two fields out of one request body instead
   of one.
