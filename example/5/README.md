# 5 — React + Array + CRUD

Connecting a real React frontend to the full-CRUD array backend from
[topic 4](../4) — CORS on the FastAPI side, add/list/reach/edit/delete
on the React side.

| Sub-topic | What it covers |
|---|---|
| [5/a — Array backend, full CRUD + CORS](a) | All five verbs from topic 4, combined into one app, plus `CORSMiddleware`. |
| [5/react — React frontend](react) | A form + list UI covering create, read, reach-by-index, update (PUT/PATCH), and delete. |

Each folder above has its own **README.md** (how to run it) and
**STEPS.md** (line-by-line code walkthrough — `5/react`'s builds up
`App.jsx` one CRUD operation at a time).
